
.body-announcement {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  

  .container {
    max-width: 1170px;
    width: 100%;

    .left_article {
      
      .article-Box {
        background-color: var(--table-background-color);
        height: 100%;
      }
    }

    .right_article {
      min-height: 380px;

      .article-Box {
        background-color: var(--table-background-color);
        height: 100%;
      }
    }
  }
}